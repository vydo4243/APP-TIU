import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function DeadlineScreen() {
  return (
    <SafeAreaView style={{ flex: 1, padding: 16 }}>
      <Text>Deadline Tab</Text>
    </SafeAreaView>
  );
}
